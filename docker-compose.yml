services:
  # 1. Datenbank
  rezept_db:
    image: postgres:15
    container_name: rezept_db
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-user}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-password}
      POSTGRES_DB: ${POSTGRES_DB:-rezept_db}
    ports:
      - "5432:5432"
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-user} -d ${POSTGRES_DB:-rezept_db}"]
      interval: 5s
      timeout: 5s
      retries: 5
    restart: always

  # 2. KI (Auskommentiert, aber Pfad für Zukunft angepasst)
  # ollama:
  #   image: ollama/ollama:latest
  #   container_name: rezept_ai
  #   ports:
  #     - "11434:11434"
  #   volumes:
  #     # ÄNDERUNG: Würde in ./data/ollama speichern
  #     - ./data/ollama:/root/.ollama
  #   environment:
  #     - OLLAMA_HOST=0.0.0.0
  #     - OLLAMA_KEEP_ALIVE=5m 
  #   deploy:
  #     resources:
  #       limits:
  #         memory: 7G 
  #       reservations:
  #         memory: 4G
  #   restart: always

  # 3. Backend
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: homechef_backend
    ports:
      - "3005:3000" # Erreichbar unter Port 3005
    depends_on:
      rezept_db:
        condition: service_healthy
      # - ollama
    environment:
      - DATABASE_URL=${DATABASE_URL:-postgresql://user:password@rezept_db:5432/rezept_db?schema=public}
      # - OLLAMA_URL=http://ollama:11434/api/generate
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - SPOONACULAR_API_KEY=${SPOONACULAR_API_KEY}
      - DEEPL_KEY=${DEEPL_KEY}
      - HF_MODEL_CACHE=/app/data/whisper-models
    volumes:
      # Whisper-Modell-Cache: Verhindert erneuten Download bei jedem Neustart (~150MB)
      - ./data/whisper-models:/app/data/whisper-models
    deploy:
      resources:
        limits:
          memory: 2G   # Whisper benötigt ~500MB während der Inferenz
        reservations:
          memory: 512M
    dns:
      - 8.8.8.8
      - 1.1.1.1
    restart: always
    healthcheck:
      test: ["CMD-SHELL", "wget --quiet --tries=1 --spider http://localhost:3000/api/recipes/ping || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - default
      - proxy-net
  
  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        - VITE_API_URL=/api 
    container_name: homechef_frontend
    ports:
      - "81:80" # Frontend erreichbar unter Port 80
    restart: always
    depends_on:
      backend:
        condition: service_healthy
    networks:
      - default
      - proxy-net

networks:
  proxy-net:
    external: true
    name: docker-proxy_proxy-netzwerk